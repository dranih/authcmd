# Feed back a denied command line to the invoking user
show_denied: true

# Allowed cmd
allowed_cmd:
  - command: id
  - command: /bin/echo
    args: # Golang regex
      forbidden: [\$]
      replace: {"pizza$":"pasta"}
  - command: ls
    args:
      allowed: [-l .*]
